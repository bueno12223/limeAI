---
trigger: always_on
---

The use of the any keyword is forbidden. Every variable, function parameter, and return value must be explicitly typed. Leverage TypeScript's advanced types and Prisma's generated types to ensure 100% type safety.